#include <iostream>
#include <cstdio>
#include <fstream>
#include <vector>
#include <cmath>
#include <algorithm>
#include <functional>
#include <numeric>
#include <cstring>
#include <sstream>
#include <queue>
#include <set>
#include <map>
#define REP(i,a,b) for(int i=int(a);i<int(b);i++)
#define all(v) (v).begin(),(v).end()
#define fill(x,i) memset(x,i,sizeof(x))
#define sz size()
#define MAX 5000
using namespace std;
int N;
double xstart,ystart,xend,yend,xleft,ytop,xright,ybottom,aux;

double A[5],B[5];
int main(){
	//freopen("in.txt","r",stdin);
	cin>>N;
	REP(i,0,N){
		cin>>xstart>>ystart>>xend>>yend>>xleft>>ytop>>xright>>ybottom;
		
		
		
		
	}
	return 0;
}